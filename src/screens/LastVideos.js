import React from 'react';
import {Text, View} from 'react-native';

const LastVideos = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Last videos Screen</Text>
    </View>
  );
};

export default LastVideos;
